{
  "name": "queima-asfalto-functions",
  "type": "module",
  "main": "lib/index.js",
  "engines": {
    "node": "24"
  },
  "scripts": {
    "build": "rm -rf lib src/shared && cp -r ../shared/officialResults src/shared && rm -f src/shared/*.test.ts && mkdir -p src/shared/shares && cp ../shared/shares/*.ts src/shared/shares && rm -f src/shared/shares/*.test.ts && mkdir -p src/shared/reminders && cp ../shared/reminders/*.ts src/shared/reminders && rm -f src/shared/reminders/*.test.ts && mkdir -p src/shared/parkrun && cp ../shared/parkrun/catalog.ts ../shared/parkrun/catalogSource.ts src/shared/parkrun && mkdir -p src/shared/account && cp ../shared/account/*.ts src/shared/account && rm -f src/shared/account/*.test.ts && mkdir -p src/shared/domain && cp ../shared/domain/*.ts src/shared/domain && rm -f src/shared/domain/*.test.ts && mkdir -p src/shared/raceCatalog && cp ../shared/raceCatalog/*.ts src/shared/raceCatalog && rm -f src/shared/raceCatalog/*.test.ts && mkdir -p src/shared/eventDiscovery && cp ../shared/eventDiscovery/*.ts src/shared/eventDiscovery && rm -f src/shared/eventDiscovery/*.test.ts && tsc",
    "watch": "npm run build && tsc --watch"
  },
  "dependencies": {
    "cheerio": "^1.1.2",
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^7.2.5",
    "pdf-parse": "^2.4.5"
  },
  "devDependencies": {
    "typescript": "~5.9.3"
  },
  "overrides": {
    "body-parser": "^1.20.6",
    "uuid": "^11.1.1"
  }
}
